import { Colors } from "../../../constants";

const BackgroundTop = () => {
  return (
    <div className="absolute w-full h-screen top-0 left-0" style={{ backgroundColor: Colors.OffWhite() }} />
  );
}

export default BackgroundTop;